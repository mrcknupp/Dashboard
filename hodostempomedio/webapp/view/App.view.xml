<mvc:View
	xmlns:mvc="sap.ui.core.mvc"
	controllerName="hodos.hodostempomedio.controller.App"
	displayBlock="true"
	xmlns="sap.m">

	<App id="idAppControl"
		 busy="{appView>/busy}"
		 busyIndicatorDelay="{appView>/delay}"/>

</mvc:View>